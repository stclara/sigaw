<fieldset>
<legend>Usuários</legend>
<br>
<%= form_tag users_path, :method => :get do %>

  <table align='center'>
  <tr align="left">
  <td>
  <label for="find_campo">Campo:</label><br>

  <%= select_tag :campo, options_for_select([["Email", "mail"],["Nome", "name"]]) %>


  </td>
  <td>
  <label for="find_chave">Chave:</label><br>
  <%= text_field_tag :chave, params[:chave], "size" => 40, "maxlenght" => 60 %>
  </td>
  <td>
  <br>
  <input type='submit' value='', class="botao"/>

  </td>
  </tr>
  </table>

<% end %>

<% if !params[:campo].nil? %>
  <div id="result">
    <%= render :partial => 'show', :locals => { :user => @users } %>
  </div>
<% end %>
<br>
<p align='right'>
<%= tooltip link_to(image_tag("add.png", {:border => 0}), new_user_path), { :tooltip => 'Adicionar novo registro.' } %>
</p>

</fieldset>
<% content_for :menu, render(:partial => './home/menuADM') -%>